/runs/
__pycache__/
.DS_Store
src/epymarl/results/
src/epymarl/tmp_results/
logs/
results/
train_results/
**/models/
# src/lare/{path,task}/models/ は **/models/ の例外で git 公開する.
# (整理済みの pretrained / finetuning モデルをここに置く運用)
!src/lare/path/models/
!src/lare/path/models/**
!src/lare/task/models/
!src/lare/task/models/**
# src/lare/{path,task}/checkpoints/ は autosave 出力. 大量蓄積されるので git 外.
src/lare/path/checkpoints/
src/lare/task/checkpoints/
.claude/settings.local.json
.claude/*.local.*
# 旧パス saved_models/ も後方互換のためサポートしているが git 管理外
src/lare/path/saved_models/
src/lare/task/saved_models/
CLAUDE.md
# collect_runs.py のローカルキャッシュ
tools/.run_cache.jsonl

# collect_runs.py が回収した学習済みモデルの受け皿
models_inbox/
tools/.notion_token

# tools/: 接続先と実験計画は各自のローカルに置く (見本を .example として公開)
tools/collect_config.yaml
tools/plan.md

# plan.md のバックアップ
tools/plan.md.bak

# 実験計画とローカル状態 (マシンごとに違う / 白が master)
tools/plans/
tools/.host_seen.json
tools/*.bak
tools/*.bak2
tools/.run_cache.jsonl.bak*

# 学習の副産物・個人設定
# (gitignore は行末コメントを解釈しないので、説明は必ず独立行に書く)
tmp_results/
.claude/skills/
